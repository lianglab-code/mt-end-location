function bp = breakpoint_detect(p)
%% BREAKPOINT_DETECT detects the breakpoints in the input series.
% To decompose the time series into several linear segments.

% Input:
% p: vector

% Output:
% bp: break points

np = numel(p);


end